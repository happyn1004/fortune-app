V48 AUTH COMPLETE FIX

핵심 수정
1) 로그인 검증 로직을 SQL의 password_hash 직접 비교 방식에서 분리
2) 비밀번호 해시를 단순 sha256 -> PBKDF2-SHA256(개별 salt, 반복 연산) 방식으로 강화
3) 기존 sha256 사용자도 로그인 성공 시 자동으로 새 방식으로 마이그레이션
4) 고객/관리자 로그인 시 세션을 먼저 clear 후 재생성하여 모바일/PC 재로그인 꼬임 방지
5) SessionMiddleware 쿠키 이름 고정, max_age 설정, HTTPS 환경 secure 적용
6) SQLite 연결에 WAL / busy_timeout / foreign_keys 적용

이번 버전의 기대 효과
- 처음 한 번만 로그인되고 두 번째부터 계정 오류가 나는 문제 완화
- 모바일과 PC에서 재로그인 실패하는 현상 완화
- 기존 계정도 비밀번호 재설정 없이 순차적으로 새 해시 체계로 전환
- Render 재시작/동시접속 환경에서 DB 잠금 안정성 향상

배포 후 권장 테스트
- 기존 고객 계정 2회 이상 로그인/로그아웃 반복
- 기존 관리자 계정 2회 이상 로그인/로그아웃 반복
- 모바일 브라우저와 PC 브라우저에서 각각 반복 테스트
- Render 재배포 후 기존 계정 재로그인 테스트
