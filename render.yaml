services:
  - type: web
    name: mysticday
    runtime: docker
    plan: starter
    autoDeploy: true
    healthCheckPath: /
    envVars:
      - key: DATA_DIR
        value: /data/mysticday
      - key: RENDER
        value: "true"
      - key: SESSION_SECRET
        generateValue: true
      - key: PYTHONUNBUFFERED
        value: "1"
    disk:
      name: mysticday-data
      mountPath: /data
      sizeGB: 1
